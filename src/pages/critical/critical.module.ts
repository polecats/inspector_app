import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CriticalPage } from './critical';

@NgModule({
  declarations: [
    CriticalPage,
  ],
  imports: [
    IonicPageModule.forChild(CriticalPage),
  ],
})
export class CriticalPageModule {}
